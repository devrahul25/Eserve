<?php
/* DEPRECATED — superseded by config.php.
 * Kept as a stub so any stale include doesn't fail loudly.
 * If you see this file in production, you can safely delete it. */
http_response_code(403);
header('Content-Type: text/plain');
exit('Forbidden');
